import{useEffect,useMemo,useState}from'react';import{Cell,Legend,Line,LineChart,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis,CartesianGrid,Bar,BarChart}from'recharts';import{getDashboard,getHotels,Hotel}from'../api';import StatCard from'../components/StatCard';import PrintButton from'../components/PrintButton';import{useRealtime}from'../useRealtime';import{Link}from'react-router-dom';
export default function Home(){const tick=useRealtime();const today=new Date().toISOString().slice(0,10);const[start,setStart]=useState(today.slice(0,8)+'01'),[end,setEnd]=useState(today),[hotelId,setHotelId]=useState(''),[data,setData]=useState<any>(null),[hotels,setHotels]=useState<Hotel[]>([]);const load=()=>getDashboard(new URLSearchParams({start,end,...(hotelId?{hotel_id:hotelId}:{})}).toString()).then(setData);useEffect(()=>{getHotels().then(setHotels)},[]);useEffect(()=>{load()},[tick]);const d=data||{kpis:{},revenue_by_hotel:[{name:'لا توجد بيانات',value:0}],paid_cash:[{name:'مدفوع',value:0},{name:'كاش',value:0}],sentiment:[{name:'إيجابي',value:0},{name:'سلبي',value:0}]};const actions=[['التقييمات','/reviews','📝'],['الحجوزات اليومية','/bookings','📅'],['الإيرادات اليومية','/revenue','💵'],['التقرير الشهري','/monthly','📊']];return <section className="page"><div className="page-head"><div><h2>لوحة أداء الفنادق</h2><p>مركز التحكم التنفيذي والتقارير السريعة</p></div><div className="actions no-print"><button className="btn primary" onClick={load}>عرض التقرير</button><button className="btn secondary" onClick={load}>تحديث البيانات</button><PrintButton label="طباعة لوحة التقرير"/></div></div><div className="filters no-print"><label>من تاريخ<input type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label>إلى تاريخ<input type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label><label>الفندق<select value={hotelId} onChange={e=>setHotelId(e.target.value)}><option value="">كل الفنادق</option>{hotels.map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select></label></div><div className="stats">{[['إجمالي التقييمات',d.kpis.reviews],['إجمالي الحجوزات',d.kpis.bookings],['الحجوزات المدفوعة',d.kpis.paid_bookings],['الحجوزات الكاش',d.kpis.cash_bookings],['الإيراد الفعلي',d.kpis.actual_revenue],['العمولة',d.kpis.commission],['صافي الإيراد',d.kpis.net_revenue],['متوسط التقييم',d.kpis.average_rating,'/10']].map((x:any)=><StatCard key={x[0]} title={x[0]} value={x[1]} unit={x[2]}/>)}</div><div className="quick-grid no-print">{actions.map(([label,to,icon])=><Link className="quick-card" to={to} key={to}><span>{icon}</span><b>فتح {label}</b></Link>)}</div><div className="chart-grid print-friendly"><div className="panel"><h3>صافي الإيراد حسب الفندق</h3><ResponsiveContainer width="100%" height={300}><BarChart data={d.revenue_by_hotel}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name" hide/><YAxis/><Tooltip/><Bar dataKey="value" name="صافي الإيراد"/></BarChart></ResponsiveContainer></div><div className="panel"><h3>مزيج المدفوع والكاش</h3><ResponsiveContainer width="100%" height={300}><PieChart><Pie data={d.paid_cash} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={95} label>{d.paid_cash.map((_:any,i:number)=><Cell key={i}/>)}</Pie><Tooltip/><Legend/></PieChart></ResponsiveContainer></div><div className="panel"><h3>اتجاه التقييمات</h3><ResponsiveContainer width="100%" height={300}><LineChart data={d.sentiment}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis allowDecimals={false}/><Tooltip/><Line type="monotone" dataKey="value" name="التقييمات"/></LineChart></ResponsiveContainer></div></div><div className="panel"><h3>أداء الفنادق</h3><div className="table-wrap"><table><thead><tr><th>الفندق</th><th>الحجوزات</th><th>مدفوع</th><th>كاش</th></tr></thead><tbody>{(d.hotel_performance||[]).map((r:any)=><tr key={r.hotel}><td>{r.hotel}</td><td>{r.bookings}</td><td>{r.paid}</td><td>{r.cash}</td></tr>)}</tbody></table></div></div></section>}

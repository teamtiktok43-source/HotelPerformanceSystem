import{FormEvent,useEffect,useState}from'react';import{createRevenue,getEmployees,getHotels,Hotel,User,getRevenue}from'../api';import PrintButton from'../components/PrintButton';import{useRealtime}from'../useRealtime';import{Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';
const safeNumber=(value:any)=>Number.isFinite(Number(value))?Number(value):0;
export default function DailyRevenue(){const tick=useRealtime();const today=new Date().toISOString().slice(0,10);const[hotels,setHotels]=useState<Hotel[]>([]),[emps,setEmps]=useState<User[]>([]),[rows,setRows]=useState<any[]>([]),[form,setForm]=useState({booking_number:'',hotel_id:'',platform:'Booking.com',revenue_date:today,actual_price:'',commissionable_amount:'',employee_id:''}),[msg,setMsg]=useState('');const load=()=>getRevenue().then(setRows);useEffect(()=>{getHotels().then(setHotels);getEmployees().then(setEmps)},[]);useEffect(()=>{load()},[tick]);const hotel=hotels.find(h=>String(h.id)===form.hotel_id);const rate=safeNumber(hotel?.commission_rate);const commission=safeNumber(form.commissionable_amount)*rate;const net=safeNumber(form.actual_price)-commission;async function save(e:FormEvent){e.preventDefault();try{await createRevenue({...form,hotel_id:Number(form.hotel_id),actual_price:Number(form.actual_price),commissionable_amount:Number(form.commissionable_amount),employee_id:form.employee_id?Number(form.employee_id):undefined});setMsg('تم حفظ الإيراد');setForm({...form,booking_number:'',actual_price:'',commissionable_amount:''});load()}catch(ex:any){setMsg(ex.message)}}const chart=rows.slice(0,12).map(r=>({name:r.hotel_name,value:r.net_revenue}));return <section className="page"><div className="page-head"><div><h2>الإيرادات اليومية</h2><p>العمولة تُحتسب تلقائيًا حسب الفندق</p></div><div className="actions no-print"><PrintButton/></div></div><form className="form-card no-print" onSubmit={save}><div className="form-grid"><label>رقم الحجز<input required value={form.booking_number} onChange={e=>setForm({...form,booking_number:e.target.value})}/></label><label>الفندق<select required value={form.hotel_id} onChange={e=>setForm({...form,hotel_id:e.target.value})}><option value="">اختر الفندق</option>{hotels.filter(h=>h.active).map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select></label><label>المنصة<input value={form.platform} onChange={e=>setForm({...form,platform:e.target.value})}/></label><label>تاريخ الإيراد<input type="date" value={form.revenue_date} onChange={e=>setForm({...form,revenue_date:e.target.value})}/></label><label>السعر الفعلي<input type="number" min="0" step="0.01" value={form.actual_price} onChange={e=>setForm({...form,actual_price:e.target.value})}/></label><label>المبلغ الخاضع للعمولة<input type="number" min="0" step="0.01" value={form.commissionable_amount} onChange={e=>setForm({...form,commissionable_amount:e.target.value})}/></label><label>نسبة العمولة<input disabled value={`${(rate*100).toFixed(2)}%`}/></label><label>العمولة<input disabled value={commission.toFixed(2)}/></label><label>صافي الإيراد<input disabled value={net.toFixed(2)}/></label><label>الموظف<select value={form.employee_id} onChange={e=>setForm({...form,employee_id:e.target.value})}><option value="">أنا</option>{emps.filter(e=>e.active).map(e=><option key={e.id} value={e.id}>{e.display_name}</option>)}</select></label></div><button className="btn primary">حفظ الإيراد</button>{msg&&<span className="inline-msg">{msg}</span>}</form><div className="report-layout print-friendly"><div className="panel"><h3>سجل الإيرادات</h3><div className="table-wrap"><table><thead><tr><th>التاريخ</th><th>الحجز</th><th>الفندق</th><th>الفعلي</th><th>العمولة</th><th>الصافي</th><th>الموظف</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.revenue_date}</td><td>{r.booking_number}</td><td>{r.hotel_name}</td><td>{safeNumber(r.actual_price).toFixed(2)}</td><td>{safeNumber(r.commission).toFixed(2)}</td><td>{safeNumber(r.net_revenue).toFixed(2)}</td><td>{r.employee_name}</td></tr>)}</tbody></table></div></div><div className="panel"><h3>صافي الإيراد</h3><ResponsiveContainer width="100%" height={320}><BarChart data={chart}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name" hide/><YAxis/><Tooltip/><Bar dataKey="value"/></BarChart></ResponsiveContainer></div></div></section>}

import{useEffect,useState}from'react';import{getMonthly}from'../api';import Stat from '../components/StatCard';import Print from '../components/PrintButton';import{Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';
function monthName(m:number){return new Intl.DateTimeFormat('ar-EG',{month:'long'}).format(new Date(2024,m-1,1))}
export default function MonthlyReport(){const now=new Date();const[y,setY]=useState(now.getFullYear()),[m,setM]=useState(now.getMonth()+1),[data,setData]=useState<any>(null);const load=()=>getMonthly(y,m).then(setData);useEffect(()=>{load()},[]);const d=data||{rows:[],totals:{}};return <section className="page"><div className="page-head"><div><h2>التقرير الشهري</h2><p>{monthName(m)} {y} — تقرير مكتوب + جداول + Charts داخل نفس المستند عند الطباعة</p></div><div className="actions no-print"><button className="btn primary" onClick={load}>عرض التقرير</button><Print/></div></div><div className="filters no-print"><label>السنة<input type="number" value={y} onChange={e=>setY(Number(e.target.value))}/></label><label>الشهر<select value={m} onChange={e=>setM(Number(e.target.value))}>{Array.from({length:12},(_,i)=><option key={i+1} value={i+1}>{monthName(i+1)}</option>)}</select></label></div><div className="stats">{[['الحجوزات',d.totals.bookings],['المدفوع',d.totals.paid],['الكاش',d.totals.cash],['الإيراد الفعلي',d.totals.actual_revenue],['العمولة',d.totals.commission],['صافي الإيراد',d.totals.net_revenue],['التقييمات',d.totals.reviews],['متوسط التقييم',d.totals.average_rating]].map((x:any)=><Stat key={x[0]} title={x[0]} value={x[1]} />)}</div><div className="report-layout print-friendly"><div className="panel"><h3>أداء الفنادق — صافي الإيراد</h3><ResponsiveContainer width="100%" height={330}><BarChart data={d.rows.slice(0,15)}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="hotel_name" hide/><YAxis/><Tooltip/><Bar dataKey="net_revenue" name="صافي الإيراد"/></BarChart></ResponsiveContainer></div><div className="panel"><h3>أداء الفنادق — الحجوزات</h3><ResponsiveContainer width="100%" height={330}><BarChart data={d.rows.slice(0,15)}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="hotel_name" hide/><YAxis/><Tooltip/><Bar dataKey="bookings" name="الحجوزات"/></BarChart></ResponsiveContainer></div></div><div className="panel print-friendly"><h3>التقرير المكتوب التفصيلي</h3><div className="table-wrap"><table><thead><tr><th>الفندق</th><th>الحجوزات</th><th>مدفوع</th><th>كاش</th><th>الإيراد</th><th>العمولة</th><th>الصافي</th><th>التقييمات</th><th>متوسط التقييم</th></tr></thead><tbody>{d.rows.map((r:any)=><tr key={r.hotel_name}><td>{r.hotel_name}</td><td>{r.bookings}</td><td>{r.paid}</td><td>{r.cash}</td><td>{r.actual_revenue.toFixed(2)}</td><td>{r.commission.toFixed(2)}</td><td>{r.net_revenue.toFixed(2)}</td><td>{r.review_count}</td><td>{r.average_rating.toFixed(2)}</td></tr>)}</tbody></table></div></div></section>}

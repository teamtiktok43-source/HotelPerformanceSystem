import{useEffect,useState}from'react';import{getRatings}from'../api';import PrintButton from'../components/PrintButton';import{useRealtime}from'../useRealtime';
export default function HotelRatings(){const tick=useRealtime();const[rows,setRows]=useState<any[]>([]);const load=()=>getRatings().then(setRows);useEffect(()=>{load()},[tick]);return <section className="page"><div className="page-head"><div><h2>تقييمات الفنادق</h2><p>ملخص متوسط التقييم وعدد المراجعات والمشاعر.</p></div><div className="actions no-print"><button className="btn secondary" onClick={load}>تحديث البيانات</button><PrintButton/></div></div><div className="panel print-friendly"><div className="table-wrap"><table><thead><tr><th>الفندق</th><th>عدد التقييمات</th><th>متوسط التقييم</th><th>إيجابي</th><th>سلبي</th><th>معلق/محايد</th></tr></thead><tbody>{rows.map(r=><tr key={r.hotel_id}><td>{r.hotel_name}</td><td>{r.review_count}</td><td>{r.average_rating.toFixed(2)}</td><td>{r.positive}</td><td>{r.negative}</td><td>{r.pending}</td></tr>)}</tbody></table></div></div></section>}

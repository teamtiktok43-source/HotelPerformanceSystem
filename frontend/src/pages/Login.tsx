import{FormEvent,useState}from'react';import{apiFetch,User}from'../api';
export default function Login({onLogin}:{onLogin:(u:User,t:string)=>void}){const[u,setU]=useState(''),[p,setP]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setErr('');setLoading(true);try{const r=await apiFetch<any>('/api/auth/login',{method:'POST',body:JSON.stringify({username:u,password:p})});onLogin(r.user,r.access_token)}catch(ex:any){setErr(ex.message)}finally{setLoading(false)}}return <div className="login-page"><form className="login-card" onSubmit={submit}><div className="login-logo">H</div><h1>Hotel Performance System</h1><p>تسجيل الدخول إلى نظام إدارة أداء الفنادق</p>{err&&<div className="alert error">{err}</div>}<label>اسم المستخدم<input autoFocus value={u} onChange={e=>setU(e.target.value)} placeholder="Mostafa"/></label><label>كلمة المرور<input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="••••••••"/></label><button className="btn primary wide" disabled={loading}>{loading?'جاري الدخول...':'تسجيل الدخول'}</button><div className="login-note">صلاحيات المستخدم تحدد الصفحات والإجراءات المتاحة.</div></form></div>}

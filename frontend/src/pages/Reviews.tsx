import{FormEvent,useEffect,useState}from'react';import{createReview,decideReview,getEmployees,getHotels,getReviews,Hotel,User}from'../api';import PrintButton from'../components/PrintButton';import{useRealtime}from'../useRealtime';
export default function Reviews({user}:{user:User}){const tick=useRealtime();const today=new Date().toISOString().slice(0,10);const[hotels,setHotels]=useState<Hotel[]>([]),[emps,setEmps]=useState<User[]>([]),[rows,setRows]=useState<any[]>([]),[form,setForm]=useState({booking_number:'',hotel_id:'',rating:'5',comment:'',sentiment:'Positive',review_date:today,proposed_action:'',employee_id:''}),[msg,setMsg]=useState('');const load=()=>getReviews().then(setRows);useEffect(()=>{getHotels().then(setHotels);getEmployees().then(setEmps)},[]);useEffect(()=>{load()},[tick]);async function save(e:FormEvent){e.preventDefault();try{await createReview({...form,hotel_id:Number(form.hotel_id),rating:Number(form.rating),employee_id:form.employee_id?Number(form.employee_id):undefined});setMsg('تم حفظ التقييم وإرساله للمراجعة');setForm({...form,booking_number:'',comment:'',proposed_action:''});load()}catch(ex:any){setMsg(ex.message)}}async function decision(id:number,status:string){try{await decideReview(id,status);load()}catch(ex:any){setMsg(ex.message)}}return <section className="page"><div className="page-head"><div><h2>إدارة التقييمات والمراجعات</h2><p>الموظف يدخل التقييم والإجراء المقترح، والمدير يعتمد أو يرفض.</p></div><div className="actions no-print"><PrintButton/></div></div><form className="form-card no-print" onSubmit={save}><div className="form-grid"><label>رقم الحجز<input required value={form.booking_number} onChange={e=>setForm({...form,booking_number:e.target.value})}/></label><label>الفندق<select required value={form.hotel_id} onChange={e=>setForm({...form,hotel_id:e.target.value})}><option value="">اختر الفندق</option>{hotels.filter(h=>h.active).map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select></label><label>التقييم<input type="number" min="0" max="10" step="0.1" value={form.rating} onChange={e=>setForm({...form,rating:e.target.value})}/></label><label>المشاعر<select value={form.sentiment} onChange={e=>setForm({...form,sentiment:e.target.value})}><option>Positive</option><option>Negative</option><option>Neutral</option></select></label><label>تاريخ التقييم<input type="date" value={form.review_date} onChange={e=>setForm({...form,review_date:e.target.value})}/></label><label>الموظف<select value={form.employee_id} onChange={e=>setForm({...form,employee_id:e.target.value})}><option value="">أنا</option>{emps.filter(e=>e.active).map(e=><option key={e.id} value={e.id}>{e.display_name}</option>)}</select></label><label className="span-2">التعليق<textarea required value={form.comment} onChange={e=>setForm({...form,comment:e.target.value})}/></label><label className="span-2">الإجراء المقترح<textarea value={form.proposed_action} onChange={e=>setForm({...form,proposed_action:e.target.value})}/></label></div><button className="btn primary">حفظ التقييم</button>{msg&&<span className="inline-msg">{msg}</span>}</form><div className="panel print-friendly"><div className="table-wrap"><table><thead><tr><th>التاريخ</th><th>الحجز</th><th>الفندق</th><th>التقييم</th><th>المشاعر</th><th>التعليق</th><th>الإجراء المقترح</th><th>الموظف</th><th>الحالة</th><th className="no-print">قرار</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.review_date}</td><td>{r.booking_number}</td><td>{r.hotel_name}</td><td>{r.rating}</td><td>{r.sentiment}</td><td>{r.comment}</td><td>{r.proposed_action}</td><td>{r.employee_name}</td><td><span className={`status ${r.status.toLowerCase()}`}>{r.status}</span></td><td className="no-print">{(user.role==='manager'||user.role==='admin')&&r.status==='Pending'?<><button className="mini ok" onClick={()=>decision(r.id,'Approved')}>اعتماد</button><button className="mini bad" onClick={()=>decision(r.id,'Rejected')}>رفض</button></>:<span>—</span>}</td></tr>)}</tbody></table></div></div></section>}

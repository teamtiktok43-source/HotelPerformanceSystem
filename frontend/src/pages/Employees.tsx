import{useEffect,useState}from'react';import{createEmployee,getEmployees,updateEmployee,User}from'../api';
export default function Employees({user}:{user:User}){const[rows,setRows]=useState<User[]>([]),[f,setF]=useState({username:'',password:'',display_name:'',role:'employee'});const load=()=>getEmployees().then(setRows);useEffect(()=>{load()},[]);async function add(){await createEmployee({...f,active:true});setF({username:'',password:'',display_name:'',role:'employee'});load()}async function toggle(u:User){await updateEmployee(u.id,{active:!u.active});load()}return <section className="page"><div className="page-head"><div><h2>إدارة الموظفين</h2><p>المستخدمون والصلاحيات وحالة الحساب.</p></div></div>{user.role==='admin'&&<div className="form-card no-print"><div className="form-grid"><label>اسم المستخدم<input value={f.username} onChange={e=>setF({...f,username:e.target.value})}/></label><label>كلمة المرور<input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><label>الاسم الظاهر<input value={f.display_name} onChange={e=>setF({...f,display_name:e.target.value})}/></label><label>الصلاحية<select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="employee">Employee</option><option value="manager">Manager</option><option value="admin">Admin</option></select></label></div><button className="btn primary" onClick={add}>إضافة موظف</button></div>}<div className="panel"><div className="table-wrap"><table><thead><tr><th>#</th><th>المستخدم</th><th>الاسم</th><th>الصلاحية</th><th>الحالة</th><th className="no-print">إجراء</th></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td>{u.id}</td><td>{u.username}</td><td>{u.display_name}</td><td>{u.role}</td><td>{u.active?'Active':'Inactive'}</td><td className="no-print"><button className="mini" onClick={()=>toggle(u)}>{u.active?'تعطيل':'تفعيل'}</button></td></tr>)}</tbody></table></div></div></section>}

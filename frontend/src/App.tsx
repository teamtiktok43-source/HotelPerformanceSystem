import{useEffect,useState}from'react';import{Routes,Route,useNavigate}from'react-router-dom';import{apiFetch,getAuthUser,logout,saveAuth,User}from'./api';import Layout from'./components/Layout';import Login from'./pages/Login';import Home from'./pages/Home';import DailyBookings from'./pages/DailyBookings';import DailyRevenue from'./pages/DailyRevenue';import Reviews from'./pages/Reviews';import HotelRatings from'./pages/HotelRatings';import MonthlyReport from'./pages/MonthlyReport';import Hotels from'./pages/Hotels';import Employees from'./pages/Employees';import Data from'./pages/Data';
export default function App(){const[user,setUser]=useState<User|null>(getAuthUser());const nav=useNavigate();useEffect(()=>{if(user)apiFetch<User>('/api/auth/me').catch(()=>{logout();setUser(null);nav('/login')})},[]);if(!user)return <Routes><Route path="*" element={<Login onLogin={(u,t)=>{saveAuth(u,t);setUser(u);nav('/')}}/>}/></Routes>;return <Layout user={user} onLogout={()=>{logout();setUser(null);nav('/login')}}><Routes><Route path="/" element={<Home/>}/><Route path="/bookings" element={<DailyBookings/>}/><Route path="/revenue" element={<DailyRevenue/>}/><Route path="/reviews" element={<Reviews user={user}/>}/><Route path="/ratings" element={<HotelRatings/>}/><Route path="/monthly" element={<MonthlyReport/>}/><Route path="/hotels" element={<Hotels user={user}/>}/><Route path="/employees" element={<Employees user={user}/>}/><Route path="/data" element={<Data/>}/><Route path="*" element={<Home/>}/></Routes></Layout>}
